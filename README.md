# claude-config-sandbox

Sample project the skills `github-cli` and `gitlab-cli` take their verification marks on.
Nothing here is used by any build. Objects created while taking a mark are deleted afterwards.
worktree probe
probe 2
